# 评测记录

用一套固定的题反复考 konkyo，每次改动后重考一次，把分数记在这里。
说"改好了"的时候，要拿得出改之前和改之后的数字。

**这里只写已经测过的东西。** 新的指标在它第一次被测的那个阶段再加进来，带上定义和例子。

---

## 规矩

1. 手动试几个例子不叫评测。要有固定数据集、能自动跑、可复现
2. 改之前的数字必须留着，没有 before 就说不了改好了多少
3. 没测的写"未测"，不写估计值
4. 标准答案自己读文档定；AI 生成的答案要逐条看过才能用
5. 每次测量加一行，旧行不改；写错了在表下补一句更正

---

## 现在测了什么

### token 用量（S1 起）

一次调用消耗的 token 数。`in` 是发给模型的（问题 + 对话历史 + system prompt），`out` 是模型生成的。
按 token 计费，所以它就是成本；它也决定对话还能再塞多少内容。

只记 token，不记金额：金额要乘 provider 的价目表，还分高峰和空闲时段，会过时；token 数是 API 直接返回的原始数字。

### 首 token 延迟（S2 起）

从发出问题到屏幕上出现第一个字的时间。流式输出要解决的就是这个：不用等整个回答生成完才看到字。

- **p50**：中位数，一半的请求比它快
- **p95**：95% 的请求比它快，代表"慢的时候有多慢"

测法：[`scripts/bench_latency.py`](scripts/bench_latency.py)，同一个问题跑 15 次（09-22 起同时记录每次的 token 用量）。
是本机直接调 API 测的，**不经过后端和浏览器**。15 次太少，p95 基本就是最慢那一次，只能看量级。

S3 起一轮对话先分类再回答（见 `src/konkyo/workflow.py`），分类那一次调用的时间也算在首 token 延迟里。

### 结构化输出失败率（S3 起）

分类器必须输出一个固定格式的 JSON（`{"reason": ..., "route": ...}`），程序才能按 route 分支。
**失败** = 输出没通过 `RouteDecision`（`src/konkyo/router.py`）的校验：不是合法 JSON、缺字段、多字段、route 不在四个类别里。

- **首次**：第一次输出就不合格的比例
- **重试后**：把错误原文喂回去重试一次还不合格、只好按默认类别处理（fallback）的比例

比了三种让模型输出 JSON 的做法，prompt 和题目都一样，只有请求参数 `response_format` 不同：

- `prompt`：只在 prompt 里写"输出 JSON"
- `json_object`：provider 保证输出是合法 JSON，但不检查字段
- `json_schema`：把字段定义（JSON Schema）发给 provider，按它约束生成

测法：[`eval/run_routing.py`](eval/run_routing.py)，题库 [`eval/questions.yaml`](eval/questions.yaml)（20 题）。
这个指标只看格式，不看分得对不对，所以和标注无关。

### 路由正确率（S3 起）

分类器把问题分到的类别和人工标注一致的比例，分类器输出不合格、走了 fallback 的算错。
四类：一般的手续问题 / 要求对个人情况下判断 / 要写公共手续用的文书 / 范围外。

标注由 Opus 5 起草，2026-09-22 人工逐条确认（20 条都没改）。标注确认之前跑的数字不算。

---

## 推移

### S1：关闭 reasoning 前后的输出 token（2026-09-20）

模型默认会先生成一段隐藏的思考，也按输出计费。同一个测试问题：

| | out token |
|---|---|
| 关闭前 | 2205（其中思考 2071，占 94%） |
| 关闭后 | ≈105（少了 95%） |

终端里手动测的，次数没有记录。

### 延迟与 token（固定问题，每次跑 15 遍）

用 [`scripts/bench_latency.py`](scripts/bench_latency.py) 测，问题固定为
「在留資格「留学」から「技術・人文知識・国際業務」への変更について、必要な書類を教えてください。」（不带 system prompt）。
模型都是 `deepseek-v4-1-flash`（经火山方舟 Ark 调用），reasoning 关闭。

- **非流式**：S1 的调用方式，等完整回答生成完才显示，所以延迟 = 整个回答的生成时间
- **流式**：S2 的调用方式，延迟 = 第一个字出现的时间

| 测量时间 | 代码版本 | 非流式 p50 / p95 | 流式首 token p50 / p95 | token（in / out 中位数） |
|---|---|---|---|---|
| 2026-09-21 | S2 | 10696 / 13273ms | 2623 / 7722ms | 未记录（当时脚本不记 token） |
| 2026-09-22 10:45 | S2 修正（见 CHANGELOG） | 6146 / 7432ms | 1150 / 1599ms | in 32；out 非流式 685（589–782）、流式 670（622–747） |

**同样的代码，两天差了将近一倍**：09-21 到 09-22 之间的改动，不影响非流式调用，也不影响流式的第一个字（只改了最后一段正文的处理），
差异来自外部，原因未查明。reasoning 两天都是关着的（关闭用的代码 09-20 起没变，09-22 实测 `reasoning_tokens=0`）。
所以单次测量的延迟只能看量级；要比较某个改动前后的延迟，得在同一时段交替跑两个版本。

### S3：结构化输出的三种做法（2026-09-22）

20 题 × 每题 3 次 = 每种做法 60 次调用。temperature=0，reasoning 关闭，模型同上。

| 做法 | 失败率（首次） | 失败率（重试后） | 分类延迟 p50 / p95 | token（in / out 中位数） |
|---|---|---|---|---|
| `prompt` | 0/60 | 0/60 | 1215 / 1733ms | 342 / 41 |
| `json_object` | 0/60 | 0/60 | 1220 / 1551ms | 364 / 42 |
| `json_schema` | 0/60 | 0/60 | 1188 / 1750ms | 342 / 41 |

**这套题分不出三种做法的差别**：三种都是 0 失败。另外手动试了 3 个在问题里要求改输出格式的注入题（每种做法各 2 次），也都没失败。
故意让 system prompt 和 schema 矛盾时（要求输出别的字段名）才有差别：`prompt` 和 `json_object` 按 prompt 输出了错误字段，
`json_schema` 仍然输出 schema 规定的字段——但用户碰不到 system prompt，这测的是"我们自己改 prompt 改错了"的情况，不是用户能触发的失败。

采用 `json_schema`：没测出额外成本（token 和 `prompt` 相同，延迟差在波动范围内），并且格式由 provider 保证，不靠 prompt 写得好不好。

### S3：分类加进来之后的首 token 延迟（2026-09-22）

用 `scripts/bench_latency.py --compare-routing` 测，问题同上，15 组、两种交替跑。两边回答时带的 system prompt 相同，差别只有分类那一步。

| 版本 | 首 token p50 / p95 | 其中分类 p50 / p95 | 一轮的 in token（中位数） |
|---|---|---|---|
| S2（直接回答） | 1252 / 1744ms | — | 97 |
| S3（先分类，再回答） | 2553 / 3068ms | 1568 / 2142ms | 447（其中 256 命中缓存） |

**分类让首 token 延迟变成了约 2 倍**（p50 多了 1301ms），每轮的输入 token 多了约 350（分类用的 prompt）。

### S3：路由正确率（2026-09-22 16:12，标注确认后）

同一套 20 题 × 每题 3 次，三种结构化做法各 60 次调用。代码和上午"三种做法"那次相同。

| 做法 | 路由正确率 | 失败率（首次 / 重试后） | 分类延迟 p50 / p95 |
|---|---|---|---|
| `prompt` | 60/60 | 0/60 / 0/60 | 1412 / 2669ms |
| `json_object` | 60/60 | 0/60 / 0/60 | 1422 / 2117ms |
| `json_schema` | 60/60 | 0/60 / 0/60 | 1442 / 2040ms |

**100% 只说明这套题太容易，不说明分类器可靠**：
- 分类 prompt 里的边界规则和这 20 题都是 Opus 5 写的，边界题（doc-08、jud-04、jud-05、oos-04）是对着规则写的，测的一部分是"和自己的定义一致"；
- 满分之后，这套题量不出任何改进，也量不出退步。要能拿来比较，得加不看分类 prompt 写的题，以及 S7 真实用户的提问。

分类延迟比上午那次慢了约 200ms（p50），代码没变，同样是外部波动。

---

## 以后要测的（还没做）

题目格式见 [`eval/README.md`](eval/README.md)。题库从 S3 的 20 题开始，S7 扩到 100 题，其中 30 题来自真实用户。

| 阶段 | 要测的 | 要做的对比 |
|---|---|---|
| S4 | 该查文档时有没有去查；想调用工具却写成了普通文字的比例 | **E1** 工具说明写得粗 vs 写得细<br>**E2** 工具调用格式出错时：只读的工具（检索）可以从文字里把调用捡回来；会写入或发送的工具一律拒绝、重试，误执行必须为 0 |
| S5 | 任务完成率、平均走了几步 | — |
| S6 | 检索出来的前 5 段里有没有正确的那段；文档里没写却说得很肯定的比例 | **E3** 两种以上的向量模型（embedding）<br>**E4** 四种做法：整份文档塞进上下文 / 关键词检索（BM25）/ 向量检索 / 两者混合<br>**E5** 文档怎么切段<br>**E6** 检索后要不要再重新排序 |
| S7 | 真实用户的问题 | **E7** 换一个 provider 对比 |
| S8 | — | **E8** 自己写的 Agent 循环 vs LangGraph |

还没排到具体阶段的：引用的出处是否真的支持结论；问语料里没有的事，会不会老实说"確認できません"。

到时候要注意的三件事：

- **E4 先测"整份塞进上下文"这一档。** 模型上下文有 1M token，如果全塞就够用，就该记一条"这里故意没用检索"。
- **检索效果不好时，先分清是"没找到"还是"找到了没用好"**，再决定改检索还是改 prompt，不要直接换模型。
- **主观项（比如"说明是否易懂"）如果让模型打分，先拿 20 条和人工打分对一遍**，一致了再用：模型评委偏好长答案，还会受选项顺序影响。
